{
  "name": "backend",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "uvicorn main:app --reload --host 0.0.0.0 --port 8000",
    "start": "uvicorn main:app --host 0.0.0.0 --port 8000",
    "build": "echo 'FastAPI does not require build step'",
    "clean": "echo 'FastAPI clean not yet configured'",
    "install:python": "pip install -r requirements.txt",
    "install:uv": "uv sync"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^24.2.1",
    "typescript": "~5.8.3"
  }
}
